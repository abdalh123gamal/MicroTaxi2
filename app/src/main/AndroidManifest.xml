<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abdalh.microtaxi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterDriver">

        </activity>
        <activity android:name=".RegisterRider">

        </activity>
        <activity android:name=".SignInDriver"></activity>
        <activity android:name=".SignInRider" >

        </activity>
        <activity android:name=".ActivitySelectType" />
        <activity android:name=".AboutMicroTaxi" />
        <activity android:name=".SettingProfile" />
        <activity android:name=".FeedBack" />
        <activity android:name=".SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>